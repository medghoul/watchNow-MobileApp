import 'package:flutter/material.dart';

class TvShowDetailScreen extends StatelessWidget {
  const TvShowDetailScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child:Text('TvShow Detail Screen'),
    );
  }
}
